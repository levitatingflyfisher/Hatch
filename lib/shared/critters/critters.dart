/// Procedural critters: `CritterSpec.of(fact)` → deterministic visual params;
/// `CritterPainter` renders them at any size from 28px (album) to 96px+
/// (hatch moment).
library;

export 'critter_painter.dart';
export 'critter_spec.dart';
