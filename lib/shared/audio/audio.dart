/// Audio substrate: the cue enum + the never-throwing, mute-law-abiding
/// service. Provider-free by design — features inject the mute getter.
library;

export 'audio_service.dart';
export 'sound_cue.dart';
